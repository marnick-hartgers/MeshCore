# Next steps for Forest UI
In the next step i like to upgrade the UI.
I need these things planned out.
- Building a dashboard / home screen
- Redesign status bar
- Remove old screens that came over from the ui-new screens. The function of those screen should be in the settings.
- Even when the companion is connected the screen may still give a small notification blink on the screen. like a icon or a small animation.

# Dashboard / home screen & status bar redesign
The home screen is suppose to be the root of where the ux flow starts. It contains some handy and use full info about the statuses of the device.
From the home screen you can see information such as:
- Status bar
  - Battery (via icon)
  - GPS status off/no-lock/lock (via icon)
  - BLE status off/connected/disconnected (via icon)
  - Buzzer on / off (via icon)
  - Unread count
- Home screen
  - Name (may be truncated or compacted in a way)
  - Toggle for GPS
  - Toggle for Buzzer
  - Stats / nice details about the radio/companion

## Stats / nice details
I would like you to make a top 10 of "things" to show on the home screen. Ill handpick them later.

## Design of the home screen
Come up with 3 designs for the home screen. 
Generate a document for every design. 
Try to make mockups using asci or asci art. 